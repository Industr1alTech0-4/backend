import { Request, Response } from 'express';

import { modelFetch } from '../model/model';

class ModelController {

    async modelFetch (req: Request, res: Response) {
        
    }

}

export const modController = new ModelController(); 